{
  "extends": "../../packages/config/tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist",
    "noEmit": false // allow emit if needed locally, but tsc --noEmit still used for check
  },
  "include": ["src/**/*", "**/*.test.ts"],
  "exclude": ["**/*.spec.ts", "**/node_modules/**", "**/dist/**"],
  "references": [{ "path": "../../packages/common" }]
}
